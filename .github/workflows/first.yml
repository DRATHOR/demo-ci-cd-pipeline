name: workflow-first
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "hello world!"
      - name: print good buy
        run: echo "done"
